# Example environment variables for backend

# Frontend origin for CORS (defaults to http://localhost:3000 if unset)
FRONTEND_ORIGIN=http://localhost:3000

# Spring profile (optional)
SPRING_PROFILES_ACTIVE=default
